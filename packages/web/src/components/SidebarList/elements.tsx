import styled from '@emotion/styled'

const SidebarContainer = styled.div`
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`

const FileListContainer = styled.div`
  overflow: hidden;
  height: 500px;
  text-overflow: ellipsis;
  white-space: nowrap;
`

export { SidebarContainer, FileListContainer }
