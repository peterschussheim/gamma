{
  "name": "shared",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-project utilities, configs and wrappers to be shared",
  "license": "MIT",
  "main": "build/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "cross-env NODE_ENV=development babel src -d build -s -w",
    "build": "cross-env NODE_ENV=production babel src -d build",
    "clean": "rimraf build"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "3.1.1",
    "babel-preset-gamma": "0.0.1"
  },
  "dependencies": {
    "dotenv": "6.0.0",
    "raven": "2.6.3"
  }
}
